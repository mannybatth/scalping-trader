export const maxDayTradesAllowed = 3;

// Minimum price needed on symbol to trigger a trade
export const minUnderlyingPrice = 10;

// Percentage of account balance to use on a single trade
export const positionSizePercentage = 0.7;

// Take profit percentage relative to the current price
export const takeProfitOnePercentage = 1.20;
export const takeProfitTwoPercentage = 1.30;

// Stop loss percentage relative to the current price
export const stopLossPercentage = 0.50;
